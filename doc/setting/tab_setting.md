# 设置页（tab_setting）

### 主页面

```
╔════════════════════════════════════════════════╗
║  设置                                ☀️  🧠   ║  ← AppBar：标题左对齐
║                                                ║     ☀️ 外观切换
║                                                ║     🧠 思考强度
║  ┌──────────────────────────────────────────┐  ║
║  │  🔑  DeepSeek 官方                    ›  │  ║  ← API 摘要卡
║  │      deepseek-v4-flash · sk-8a8••••••H9x │  ║     点击 → API 配置对话框
║  └──────────────────────────────────────────┘  ║
║  ┌──────────────────────────────────────────┐  ║
║  │  📊  用量统计                          › │  ║  ← 用量入口卡
║  └──────────────────────────────────────────┘  ║    点击 → 跳转用量统计页面
║  ┌──────────────────────────────────────────┐  ║
║  │  🎓  导师参考提示词                    ›  │  ║  ← 点击 → 提示词弹窗
║  │      复制模板，应用外生成自定义导师组       │  ║
║  └──────────────────────────────────────────┘  ║
║  ┌──────────────────────────────────────────┐  ║
║  │  ☑  大纲生成提示词                     ›  │  ║  ← 点击 → 提示词弹窗
║  │      复制后应用外生成教学大纲              │  ║
║  └──────────────────────────────────────────┘  ║
║                                                ║
║                   v1.2.0                       ║  ← 居中灰字（运行时读 pubspec）
║                                                ║
╠════════════════════════════════════════════════╣
║                                                ║
║    💬         👥          ⚙️                  ║
║    聊天       通讯录       设置                  ║  ← 页面导航
║                                                ║     点击 → 跳转其他页面
╚════════════════════════════════════════════════╝
```

### 外观菜单（AppBar 右侧按钮弹出）

```
        ┌──────────────────────────┐
        │ ✓ 明亮                   │ ← 当前档打勾；选择即生效并落盘
        │   深色                   │
        │   自动（跟随系统）        │
        └──────────────────────────┘
```

### 思考强度菜单（AppBar 右侧按钮弹出）

```
        ┌──────────────────────────────┐
        │ 教学 / 问答（可翻教材）        │  ← 分组标题（灰字，不可点）
        │ ✓ 无                         │  ← 上课对话/问答/课前问候共用，默认「无」
        │   低                         │
        │   高                         │
        │ 群聊 / 闲聊 / 更新 / 提炼      │  ← 分组标题（灰字，不可点）
        │   无                         │
        │ ✓ 低                         │  ← 课后更新/群聊生成/闲聊/提炼共用，默认「低」
        │   高                         │
        └──────────────────────────────┘
```

### API 配置对话框

```
┌────────────────────────────────────────────────┐
│  API 配置                                      │
│                                                │
│  ◉ DeepSeek 官方                       ✎  🗑   │  ← 单选钮=使用中档；尾随编辑/删除
│     DeepSeek 官方 · deepseek-v4-flash          │
│     · sk-8a8••••••H9x                          │
│                                                │
│  ○ 备用代理                            ✎  🗑   │
│     OpenAI 兼容 · deepseek-chat                │
│     · sk-ab12••••••cd34                        │
│                                                │
│                             ＋ 添加配置         │  ← 新档即选中（检验/保存直接生效）
│                                                │
│  连接成功 · deepseek-v4-flash · 342ms           │  ← 检验结果行（绿=成功含耗时）
│                                                │
│  [检验连通性]            [取消]      [保存]      │  ← 检验针对当前选中档
└────────────────────────────────────────────────┘
```

```
┌────────────────────────────────────────────────┐
│  删除配置档                                     │
│                                                │
│  确定删除「备用代理」吗？                        │
│  其包含的 API Key 将一并移除，且无法找回。       │
│                                                │
│               [取消]      [删除]                │  ← 删除钮红底警示
└────────────────────────────────────────────────┘
```

### 配置档表单（添加/编辑共用子对话框）

```
┌────────────────────────────────────────────────┐
│  添加配置                                       │
│                                                │
│  配置名称                                       │
│  ┌──────────────────────────────────────────┐  │
│  │ DeepSeek 官方                            │  │  ← 防重名
│  └──────────────────────────────────────────┘  │
│  服务商                                         │
│  ┌──────────────────────────────────────────┐  │
│  │ DeepSeek 官方                         ▾  │  │  ← 下拉：DeepSeek 官方 / OpenAI 兼容
│  └──────────────────────────────────────────┘  │
│  接口 https://api.deepseek.com（内置）          │  ← deepseek 时（灰字说明，不可编辑）
│  Base URL                                      │  ← openai 时替换上行：自填输入框，
│  ┌──────────────────────────────────────────┐  │     输入实时裁剪 /v1 后路径
│  │ https://api.example.com/v1               │  │
│  └──────────────────────────────────────────┘  │
│  Model                                         │
│  ┌──────────────────────────────────────────┐  │
│  │ deepseek-v4-flash                        │  │  ← 预填可改（编辑沿用档内值）
│  └──────────────────────────────────────────┘  │
│  API Key                                       │
│  ┌──────────────────────────────────────────┐  │
│  │ ••••••••••••••••                         │  │  ← 遮显输入
│  └──────────────────────────────────────────┘  │
│                                 检验连通性      │  ← 当前填写即测，不必先保存
│  连接成功 · deepseek-v4-flash · 342ms（绿）     │
│                                                │
│                [取消]        [确定]             │
└────────────────────────────────────────────────┘
```

数据存储（CONFIG.json）

```json
{
  "profiles": [
    {
      "id": "1730000000000-42",
      "name": "DeepSeek 官方",
      "provider": "deepseek",
      "apiUrl": "https://api.deepseek.com",
      "model": "deepseek-v4-flash",
      "apiKey": "enc1:..."
    }
  ],
  "active": "1730000000000-42",
  "themeMode": "system",
  "teachingThinkingEffort": "disabled",
  "textThinkingEffort": "low"
}
```

### 提示词弹窗（导师参考提示词 / 大纲生成提示词共用）

```
┌────────────────────────────────────────────────┐
│  导师参考提示词                                 │
│  ┌──────────────────────────────────────────┐  │
│  │ # 导师参考模板                            │  │
│  │ ……                                       │  │  ← 提示词全文，SelectableText
│  │ ……（可滚动、可选中）                       │  │     可选中复制
│  └──────────────────────────────────────────┘  │
│  ⧉ 复制全文                    [关闭]          │
└────────────────────────────────────────────────┘
```
