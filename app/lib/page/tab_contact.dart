import 'package:flutter/material.dart';

class TabContact extends StatelessWidget {
  const TabContact({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('通讯'));
  }
}
